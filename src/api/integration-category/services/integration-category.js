'use strict';

/**
 * integration-category service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::integration-category.integration-category');
